---
title: Cypress
id: cypress
slug: /quickstart/cypress
sidebar_position: 2
---

import {
  InstallDevDep,
  HelpSection,
  Enjoy,
  SetUpProjectInArgos,
  AddSecret,
} from "@site/src/partials";

# Cypress Quickstart

Learn how to connect your Cypress project to Argos.

## Prerequisites

Before starting, ensure that:

- Cypress [is installed](https://docs.cypress.io/guides/getting-started/installing-cypress) in your project.
- A CI/CD setup is in place to run your E2E tests.
- You know how to add a environment variables (ie. secret) to your CI.

## 1. Install packages

<InstallDevDep dependency="@argos-ci/cli @argos-ci/cypress" />

Learn more about [Argos CLI](/argos-cli) and [@argos-ci/cypress](/cypress).

## 2. Update Cypress config file

Copy/past this code at the end of your `cypress/support/index.js` file.

```js cypress/support/index.js
import "@argos-ci/cypress/support";
```

## 3. Capture screenshots

In your tests, use `argosScreenshot` to capture stable screenshots and send them to Argos.

For example:

```js title="cypress/e2e/homepage.cy.js"
it("screenshot homepage", async ({ page }) => {
  cy.visit("https://localhost:3000");
  cy.argosScreenshot("homepage");
});
```

#### TypeScript support

To add typings for TypeScript, update your `tsconfig.json`:

```js
{
  "compilerOptions": {
    "types": ["cypress", "@argos-ci/cypress"]
  }
}
```

## 4. Set up a Project in Argos

<SetUpProjectInArgos />

## 5. Setup your CI

<AddSecret />

<Enjoy />

---

<HelpSection />
