---
slug: /quickstart/cypress
title: Cypress
sidebar_position: 2
---

import {
  AddSecret,
  Congratulation,
  AdditionalResources,
  HelpSection,
  InstallDevDep,
  SetUpProjectInArgos,
} from "@site/src/partials";

# Cypress Quickstart

How to connect your Cypress project to Argos.

## Prerequisites

Before starting, ensure that:

- Cypress [is installed](https://docs.cypress.io/guides/getting-started/installing-cypress) on your project
- A [CI/CD run your E2E tests](https://playwright.dev/docs/ci-intro#on-pushpull_request)

## 1. Install packages

<InstallDevDep dependency="@argos-ci/cli @argos-ci/cypress" />

## 2. Update Cypress config file

Copy/past this code at the end of your `cypress/support/index.js` file.

```js cypress/support/index.js
import "@argos-ci/cypress/support";
```

## 3. Capture screenshots

In your E2E tests, use `argosScreenshot` to capture stable screenshots.

For example:

```js title="cypress/e2e/homepage.cy.js"
it("screenshot homepage", async ({ page }) => {
  cy.visit("https://localhost:3000");
  cy.argosScreenshot("homepage");
});
```

**Note:** Consider adding `/cypress/screenshots` directory in your `.gitignore` file, to avoid uploading screenshots to your Git repository.

### TypeScript support

To add typings for TypeScript, update your `tsconfig.json`:

```js
{
  "compilerOptions": {
    "types": ["cypress", "@argos-ci/cypress"]
  }
}
```

## 4. Set up a Project in Argos

<SetUpProjectInArgos />

## 5. Setup your CI

<AddSecret />

<Congratulation />

<AdditionalResources>
  <li>
    <a href="/cypress">@argos-ci/cypress package doc</a>
  </li>
</AdditionalResources>

---

<HelpSection />
