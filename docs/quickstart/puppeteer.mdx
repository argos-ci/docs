---
slug: /quickstart/puppeteer
title: Puppeteer
sidebar_position: 3
---

import {
  AdditionalResources,
  AddSecret,
  Congratulation,
  HelpSection,
  InstallDevDep,
  ScreenshotGuidesLink,
  SetUpProjectInArgos,
} from "@site/src/partials";

# Puppeteer Quickstart

Learn how to connect your Puppeteer project to Argos.

## Prerequisites

Before starting, ensure that:

- Puppeteer [is installed](https://pptr.dev/#getting-started)
- A CI/CD run your E2E tests

## 1. Set up a Project in Argos

<SetUpProjectInArgos />

## 2. Capture screenshots

<InstallDevDep dependency="@argos-ci/cli @argos-ci/puppeteer" />

#### II. Update your E2E tests

Use <code>argosScreenshot</code> helper to capture stable screenshots in your E2E tests.

For example:

```js
import { argosScreenshot } from "@argos-ci/puppeteer";
const puppeteer = require("puppeteer");

(async () => {
  const browser = await puppeteer.launch();
  const page = await browser.newPage();
  await page.goto("http://example.com");
  await argosScreenshot(page, "example.png");
  await browser.close();
})();
```

<ScreenshotGuidesLink />

## 4. Setup your CI

<AddSecret />

<Congratulation />

<AdditionalResources>
  <li>
    <a href="/puppeteer">@argos-ci/puppeteer package doc</a>
  </li>
</AdditionalResources>

---

<HelpSection />
