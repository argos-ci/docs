---
title: GitLab CI
id: gitlab-ci
slug: /gitlab-ci
---

import {
  WhyArgosToken,
  GetArgosToken,
  TokenPrivacyWarningMessage,
} from "@site/src/components/ArgosToken";

# Use Argos with GitLab CI

Configure [GitLab CI](https://docs.gitlab.com/ee/ci/) to send screenshots to Argos.

## Configuring environment variable

<WhyArgosToken />
<GetArgosToken />

In your GitLab repository, go to Settings > CI/CD > and then expand the Variables section. Then, set the `ARGOS_TOKEN` as an environment variable.

<TokenPrivacyWarningMessage />

If you are not comfortable with storing secret, read Gitlab CI documentation about [adding a CI/CD variable to a project](https://docs.gitlab.com/ee/ci/variables/#add-a-cicd-variable-to-a-project).

## Configure GitLab CI

```yml title=".gitlab-ci.yml"
script:
  # 👉 Insert the steps required to take screenshots

  # Upload screenshots to argos-ci.com
  - npx @argos-ci/cli upload ./screenshots
```

We don't have GitLab CI example repository to yet. Don't hesitate to contribute by [adding an example](https://github.com/argos-ci/argos/tree/main/examples)!
