---
title: Drone CI
id: drone-ci
slug: /drone-ci
---

import {
  GetArgosToken,
  WhyArgosToken,
  SetArgosToken,
  TokenPrivacyWarningMessage,
} from "@site/src/components/ArgosToken";

# Use Argos with Drone CI

Configure [Drone CI](https://www.drone.io/) to send screenshots to Argos visual testing platform.

## Configuring environment variable

<WhyArgosToken />
<GetArgosToken />
<SetArgosToken />
<TokenPrivacyWarningMessage />

If you are not comfortable with storing secret, read [Drone CI documentation about repository secret](https://docs.drone.io/secret/repository/).

## Configure Drone CI

```yml title=".drone.yml"
kind: pipeline
type: docker
name: default

steps:
  # 👉 Insert the steps required to take screenshots

  - name: Upload screenshots to argos-ci.com
    commands:
      - npx @argos-ci/cli upload ./screenshots
    secrets: [ARGOS_TOKEN]
```

We don't have Drone CI example repository to yet. Don't hesitate to contribute by [adding an example](https://github.com/argos-ci/argos/tree/main/examples)!
