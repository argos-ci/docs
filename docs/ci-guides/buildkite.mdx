---
title: Buildkite
id: buildkite
slug: /buildkite
sidebar_position: 10
---

import {
  InstallCliCommand,
  RepositoryExampleLink,
  Enjoy,
} from "@site/src/partials";
import TokenStep from "./_token-step.mdx";

# Use Argos with Buildkite

Configure [Buildkite](https://buildkite.com/) to send screenshots to Argos visual testing platform.

## 1. Install `@argos-ci/cli`

<InstallCliCommand />

## 2. Set `ARGOS_TOKEN` as CI secret

<TokenStep />

### Expose `ARGOS_TOKEN` as environment variable

In your Buildkite pipeline, go to **Settings -> Steps -> Environment Variables**. Set the `ARGOS_TOKEN` as an environment variable and paste the value you stored in the previous step.

> If you are not comfortable with storing secrets, read the [Buildkite documentation](https://buildkite.com/docs/pipelines/secrets#using-a-secrets-storage-service).

## 3. Upload the screenshots

Add the following command to your CI workflow to upload screenshots to Argos:

```sh
npx @argos-ci/cli upload ./screenshots
```

#### Example

```yml title="pipeline.yml"
steps:
  # 👉 Insert the steps required to take screenshots

  - label: "Upload screenshots to argos-ci.com"
    command: npx @argos-ci/cli upload ./screenshots
```

<RepositoryExampleLink />

## Enjoy!

<Enjoy />
