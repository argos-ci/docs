---
title: Buildkite
id: buildkite
slug: /buildkite
---

import {
  WhyArgosToken,
  GetArgosToken,
  SetArgosToken,
} from "@site/src/components/ArgosToken";

# Use Argos with Buildkite

Configure [Buildkite](https://buildkite.com/) to send screenshots to Argos.

## Set up repository token

<WhyArgosToken />
<GetArgosToken />
<SetArgosToken />

## Configure Buildkite

```yml
steps:
  # 👉 Insert the steps required to take screenshots

  - label: "Upload screenshots to argos-ci.com"
    command: npx @argos-ci/cli upload ./screenshots
```

We don't have Buildkite example repository to yet. Don't hesitate to contribute by [adding an example](https://github.com/argos-ci/argos/tree/main/examples)!
