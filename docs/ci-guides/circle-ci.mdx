---
title: CircleCI
id: circle-ci
slug: /circle-ci
sidebar_position: 20
---

import { InstallCliCommand, Enjoy } from "@site/src/partials";
import circleCiEnvVariable from "@site/static/img/circle-ci-env-variable.png";
import TokenStep from "./_token-step.mdx";

# Use Argos with CircleCI

Configure [CircleCI](https://circleci.com/) to send screenshots to Argos visual testing platform.

## 1. Install `@argos-ci/cli`

<InstallCliCommand />

## 2. Set `ARGOS_TOKEN` as CI secret

<TokenStep />

### Expose `ARGOS_TOKEN` as environment variable

In CircleCI go to **Project settings -> Environment Variables -> Add Environment Variable**. Set `ARGOS_TOKEN` as name, and your token as value.

<img
  src={circleCiEnvVariable}
  alt="CircleCI setup environment variable"
  className="rounded"
  style={{ marginBottom: 20 }}
/>

> If you are not comfortable with storing secret, read [CircleCI documentation](https://circleci.com/docs/env-vars/).

## 3. Upload the screenshots

Add the following command to your CI workflow to upload screenshots to Argos:

```sh
npx @argos-ci/cli upload ./screenshots
```

#### Example

```yml title=".circleci/config.yml"
jobs:
  build:
    executor: node/default
    steps:
      - checkout

      # 👉 Insert the steps required to take screenshots

      - run:
          name: Upload screenshots to argos-ci.com
          command: npx @argos-ci/cli upload ./screenshots

workflows:
  ci:
    jobs:
      - build
```

<RepositoryExampleLink />

## Enjoy!

<Enjoy />
