---
title: Review changes
id: review-changes
slug: /review-changes
sidebar_position: 5
---

import githubCommitStatus from "@site/static/img/github-commit-status.png";
import buildExample from "@site/static/img/build-example.png";

# Review changes

Once [installation](/installation) is complete, Argos will receive screenshots of your application when you create a pull request or merge a commit to the repository's [reference branch](#reference-branch-definition).

Argos will notify you directly on your GitHub pull request by adding a status check. If no changes are detected, the status is ✅ Pass, and you can confidently merge your pull request.

If changes are detected, the status is ❌ Fail. In this case, you must review your visual changes.

<img src={githubCommitStatus} alt="GitHub commit status" className="rounded" />

> Click on the **"Details"** link to review the visual changes.

## Reviewing visual changes

The visual review takes place in the Argos application.

<img
  src={buildExample}
  alt="Example build"
  style={{ borderRadius: 8, marginBottom: 10 }}
/>

Navigate through the screenshot diffs by clicking on the sidebar, the navigation buttons, or using your keyboard.

> Tip: Press `?` to show all available keyboard shortcuts.

The side-by-side UI is designed to quickly spot visual changes and verifies that screenshots match their expected results.

#### 👎 Reject a build

If you spot a visual bug or if a screenshot doesn't meet your expectations, click on the "Review Changes" button and select "Reject Changes". The build status will become "Changes Rejected" and GitHub will be notified of the rejection.

After rejecting the change, you should commit a new version to fix it.

#### 👍 Approve a build

When screenshots match the expected results, click on the "Review Changes" button and select 🟢&nbsp;"Approve Changes". The build status will become 🔴&nbsp;"Changes Approved" and GitHub will be notified of the approval.

Once the changes are accepted, you can confidently merge your pull request.

## FAQ

<details>
  <summary>How do I update the screenshot comparison baseline?</summary>
  <p>
    Argos updates the comparison baseline screenshots when a commit is merged
    into the repository's reference branch. You have to run Argos on your
    reference branch.
  </p>
</details>

<details id="reference-branch-definition">
  <summary>What is a reference branch?</summary>
  <p>
    Each build from a reference branch is used as the baseline for the
    screenshots comparisons. Argos automatically uses the GitHub default branch
    as the reference branch. You can set another reference branch in Argos
    repository settings.
  </p>
</details>

<details>
  <summary>How to change the reference branch?</summary>
  <p>You can set another reference branch in Argos repository settings.</p>
</details>

<details>
  <summary>What is an orphan build?</summary>
  <p>
    An orphan build is the first one performed on a reference branch. It won't
    show any comparison. The screenshots of this build will be considered as the
    baseline for the following comparisons.
  </p>
</details>

<details>
  <summary>What is a reference build?</summary>
  <p>
    A reference build is a build created from a CI running on a commit pushed on
    your reference branch. Reference build are used as baseline for branches
    created from this commit. Argos automatically choose the best reference
    build by analyzing commits history.
  </p>
</details>
