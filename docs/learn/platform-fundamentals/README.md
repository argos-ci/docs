# Platform fundamentals

The core concepts behind how Argos turns screenshots into a reliable visual review signal. Start here to understand what Argos compares, how it picks a baseline, and the modes a build can run in.

* [How Argos detects visual differences](how-argos-detects-visual-differences.md) — the comparison algorithm Argos uses to surface meaningful visual changes.
* [Baseline build](baseline-build.md) — how Argos chooses the reference build that each new build is compared against.
* [Build modes](build-modes.md) — the difference between CI mode and monitoring mode, and when to use each.
